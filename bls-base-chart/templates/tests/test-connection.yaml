apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bls-base.fullname" . }}-test-connection"
  labels:
    {{- include "bls-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bls-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

